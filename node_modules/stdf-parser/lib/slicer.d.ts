/// <reference types="node" />
/// <reference types="node" />
import { TransformStream } from 'stream/web';
import { EnumEndian } from './constants';
export interface ISlice {
    offset: number;
    buffer: Buffer;
}
export declare class StdfSlicer extends TransformStream<Blob, ISlice> {
    private buffer;
    private _offset;
    private _endian;
    constructor();
    get endian(): EnumEndian;
    get offset(): number;
    private doParse;
}
