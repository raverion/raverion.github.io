import { BufferReader } from './buffer-reader';
import { EnumRecordType } from './constants';
export declare class StdfRecord {
    readonly type: number;
    readonly sub: number;
    readonly subType: EnumRecordType;
    constructor(type: number, sub: number, bufferReader: BufferReader);
}
