import { BufferReader } from '../buffer-reader';
import { StdfRecord } from '../stdf-record';
export declare class DtrRecord extends StdfRecord {
    textDat?: string;
    constructor(type: number, sub: number, br: BufferReader);
}
