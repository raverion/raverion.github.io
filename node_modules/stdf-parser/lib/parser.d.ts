/// <reference types="node" />
/// <reference types="node" />
import { Transform } from 'stream';
import { EnumEndian } from './constants';
import { StdfFile } from './stdf-file';
export declare class StdfParser extends Transform {
    private buffer;
    private _offset;
    private _endian;
    constructor();
    get endian(): EnumEndian;
    get offset(): number;
    static parse(buffer: Buffer): StdfFile;
    static stream(): StdfParser;
    static isStdfV4(buffer: Buffer): boolean;
    private doParse;
}
